# Git Useful Commands (CLI):-
1. git init
2. git add f1.md
3. git rm --cached f1.md
4. git restore --stages filenmae.extension

# Commits
1. git commit -m "commit message"
2. git commit --amend -m "
2. git commit --amend --no-edit



# To connect Local repository (GENAI  folder ) to 
git remote add origin https://github.com/shubham787u/Gen-AI.git

# To upload our code to github
git push -u origin main